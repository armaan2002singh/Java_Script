"use strict"; //treat all JS code as newer version

// alert(3+3)
// We are using node.js not browser so we can't use this syntax for alert
console.log(3+3); console.log("Hitesh");
// code readability should be high

// https://tc39.es/ecma262/

let name = "hitesh"
let age = 18
let isLoggedIn = false
let state; //undefined
let city = null; // null
// number => 2^53
// bigint (for larger number)
// string => "",'' => double quotes is prefered
// boolean => true/false
// null => standalone value => representation for empty value , it is a special type , we can assign it to any variable
// undefined => value is not defined =>variable declared but not defined
// symbol => used in implementation for react , for defining uniqueness
// object 

console.log(typeof null); //null is object 
console.log(typeof undefined); //undefined

// specifications